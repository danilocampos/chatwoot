<% if ENV.fetch('ENABLE_ACCOUNT_SEEDING', !Rails.env.production?)  %>
<section class="main-content__body">
 <hr/>
  <%= form_for([:seed, namespace, page.resource], method: :post, html: { class: "form" }) do |f| %>

  <div class="form-actions">
    <div class="pb-3">
      <p>Clique no botão para gerar dados de exemplo nesta conta para demonstrações.</p>
      <p class="text-color-red">Atenção: isso vai apagar todos os dados existentes nesta conta.</p>
    </div>
  <%= f.submit 'Gerar dados de exemplo' %>
  </div>
  <% end %>
</section>
<% end %>
